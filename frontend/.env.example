# API Configuration
VITE_API_URL=http://localhost:5001

# Example environment variables
# VITE_API_URL - The base URL for the backend API
# Must start with VITE_ to be exposed to the client-side code 